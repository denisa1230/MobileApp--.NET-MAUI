@page
@model Proiect.Pages.Reservations.IndexModel

@{
    ViewData["Title"] = "Index";
}


<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Reservation[0].Member)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Reservation[0].Bus.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Reservation[0].DepartureDate)
            </th>
            <th>
                @Html.DisplayNameFor(model =>model.Reservation[0].Bus.Price)
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Reservation) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Member.FullName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Bus.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DepartureDate)
            </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Bus.Price)
                </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
